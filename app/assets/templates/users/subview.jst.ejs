<% usersList.each(function (user) { %>
<li id="follow" data-id="<%= user.id %>">
	<a href="/#/users/<%= user.id %>">
		<img src="<%= user.escape("photo_thumb") %>">
	</a>
	
	<ul class="user-info">
		<li class="username">
			<a href="/#/users/<%= user.id %>"><%= user.escape("username") %></a>
			<!--FOLLOW STATUS -->
		</li>
		
		<li>
			<a href="/#/users/<%= user.id %>">
				currently watching
				<strong><%= user.escape("currentCount") %></strong> shows
			</a>
		</li>
		
		<li>
			<a href="/#/users/<%= user.id %>">
				<span class="heart"></span>
				<p><%= user.escape("favoritesCount") %> favorite shows</p>
			</a>
		</li>
		
	</ul>
</li>
<% }) %>