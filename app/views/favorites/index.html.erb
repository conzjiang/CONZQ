<%= render partial: "users/profilenav" %>

<h1><%= @user.username %>'s Favorites</h1>

<ul class="show_list">
  <% @favorites.each do |fav_show| %>
    <li>
			<%= render partial: "result", locals: { tv: fav_show } %>
		</li>
  <% end %>
</ul>